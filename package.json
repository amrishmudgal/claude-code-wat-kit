{
  "name": "claude-code-wat-kit",
  "version": "0.9.0",
  "private": true,
  "description": "One-file project bootstrap for Claude Code. Source in kit/, installer in dist/CLAUDE.md.",
  "type": "module",
  "license": "MIT",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "node scripts/build-bootstrap.mjs kit dist/CLAUDE.md",
    "test": "node tests/kit-lint.mjs && node tests/guard.test.mjs && node tests/roundtrip.mjs"
  }
}
